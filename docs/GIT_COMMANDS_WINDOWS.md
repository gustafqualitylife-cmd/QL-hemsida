# Git – Quick Push (Windows / PowerShell)

Använd detta när du vill pusha ändringar snabbt.
Kopiera exakt som det är och klistra in i terminalen.

---

## Standard: add → commit → push

```powershell
git add .
git commit -m "Describe what you changed"
git push origin main



Exempel på commit-meddelanden
git commit -m "Update booking schema"

git commit -m "Add seller file upload"

git commit -m "Fix booking status flow"